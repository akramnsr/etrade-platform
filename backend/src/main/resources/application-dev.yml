spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/etrade_db
    username: etrade_user
    password: etrade_password

logging:
  level:
    com.portnet.etrade: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
